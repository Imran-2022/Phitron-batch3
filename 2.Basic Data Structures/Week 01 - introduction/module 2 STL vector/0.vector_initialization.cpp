#include <bits/stdc++.h>
using namespace std;
int main()
{
    //  type one.

    // vector <int> v;
    // cout<<v.size()<<endl;

    // type two

    // vector <int> v(5);
    // cout<<v.size();

    // type three

    // int n;
    // cin >> n;
    // vector<int> v(n, 5);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << endl;
    // }

    // type four

    // vector<int> v2(v);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v2[i]+2 << endl;
    // }

    // type five

    // int ar[5]={1,2,3,4,5};
    // vector<int>v(ar,ar+5);
    // cout<<v.size()<<endl;

    // we can to this also -  vector<int> v={1,2,3,4}// not need to mention size, as it is dynamic array.
    
    return 0;
}